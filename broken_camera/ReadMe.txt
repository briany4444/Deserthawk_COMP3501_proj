To Build:
open in Virtual Studio Code version 2018 or above
Change the library path in cmake, change the path in path_config.h to the files path(broken_camra),
set the configuration to  debug x86
set main as the start up item

You reguire a system tha can run geometry shaders